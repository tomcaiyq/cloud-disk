# port
server:
  port: 8888

spring:
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://106.13.41.208:3306/test?serverTimezone=UTC&useSSL=false&characterEncoding=utf-8
    username: root
    password: Cai@1995
  # redis
  redis:
    host: 121.36.32.117
    port: 6379
    password: 123456

# mybatis
mybatis:
  # mapper文件路径
  mapper-locations: classpath:mapper/*.xml
  # 别名
  type-aliases-package: com.tomcai.redis.pojo
  configuration:
    # 驼峰命名
    map-underscore-to-camel-case: true
